// import ItemsPage from "./Items";

export default function ReportsPage() {
  return (
    <div>
      <p>tela de reports</p>
      {/* aqui vamos ter 3 botões que vão redirecionar o usuário pra:
      - Items Report <ItemsPage />
      - Daily Units Report que ainda não existe
      - Main Product Inspector que também não existe
      Ou seja, por enquanto só temos o relatório de items */}
    </div>
  );
}
